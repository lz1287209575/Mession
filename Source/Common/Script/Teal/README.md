# Teal submodule placeholder

实际代码由 git submodule 提供:

```
git submodule add https://github.com/teal-language/tl Source/Common/Script/Teal
```

tlc 编译器 / tl-lsp / tl_check 在 CI / 开发者本地使用,不在 Mession 二进制中。
本目录在 .gitmodules 注册前为空。