# MobDebug submodule placeholder

实际代码由 git submodule 提供:

```
git submodule add https://github.com/lunarmodules/mobdebugd Source/Common/Script/MobDebug
```

MobDebug 由 Lua 运行时通过 require 加载,**不编译进 Mession 二进制**。
本目录在 .gitmodules 注册前为空。