# Async / Promise / Coroutine

这份文档记录项目内异步设施的定位。

## 当前定位

项目内的异步能力主要用于：

- 把任务投递到后续 tick
- 在调用链之间传递未来结果
- 提供轻量的协程风格编排

## 当前原则

- 先服务当前事件循环模型
- 不为了语法新颖度强推复杂协程方案
- 保持零外部依赖和工程可控性

## 当前建议

如果要用异步能力，优先问三个问题：

1. 这件事是否真的需要跨 tick
2. 是否真的需要跨线程
3. 是否可以先用更简单的任务投递解决
