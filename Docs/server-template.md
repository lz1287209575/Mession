# Server Template

Mession 各服当前共享同一套运行模板。

## 当前结构

典型服务器都在统一框架下完成：

- 初始化配置
- 注册监听
- 驱动主循环
- 驱动网络和任务子循环
- Tick 后端连接
- 执行关服清理

## 当前意义

这套模板的价值是：

- 各服生命周期一致
- 新增服务器时不需要重新发明主循环
- 底层 IO 与业务逻辑边界更清晰

## 建议

新增服务时，优先沿用现有模板，而不是自行实现另一套生命周期管理。
