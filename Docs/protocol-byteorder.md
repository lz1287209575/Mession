# Protocol Byte Order

这份文档记录 Mession 当前字节序策略。

## 当前状态

当前并没有完成全量统一网络字节序。  
项目处于“逐步收敛”的状态：

- 一部分跨服消息已经按网络序处理
- 客户端侧历史脚本和现有联调路径仍依赖当前约定

## 当前原则

- 不为“形式统一”一次性打断全部主链路
- 协议字节序调整必须配套脚本验证
- 每次迁移优先选择收益高、影响可控的消息

## 建议

只有在下面两点同时满足时，再继续推进：

1. 具体消息范围已确定
2. `verify_protocol.py` 和 `validate.py` 已同步更新
